<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/index.css">
    <title>Job Hunter</title>
    
</head>
<body>
    <div class="header">
        <h1>Job Hunter</h1>
        <div>
            <a href="/user/<%=user.id%>"><p>Your Jobs</p></a>
            <form action="/logout?_method=DELETE" method="POST">
                <button type="submit" id="logout">Logout</button>
            </form>
        </div>
    </div>
    
    <div class="feature">
        <img src="/images/undraw_interview_re_e5jn.svg" alt="" id="main-image">
        <div class="feature-text">
            <h1>The Job of Your Dreams</h1>
            <h2>Now in the palm of your hands</h2>
        </div>
    </div>
    
    
    <div class="container">
        <% jobs.forEach(job => { %>
            <div class="card">
                <div class="card-body">
                    <div class="card-title"><strong><%=job.role%></strong></div>
                    <div class="card-subtitle"><%=job.company%></div>
                    <div class="card-salary"><b>Salary:    <%=job.salary %></b></div>
                    <h3>Description:</h3>
                    <div  class="card-description"><%=job.description %></div>
                </div>
                <div class="footer">
                    <form action="/user/<%=user.id%>/<%=job.id%>?_method=PUT" method="POST"><button id="apply">Apply</button></form>
                </div>
            </div>
        <% }) %>
    </div>
    
</body>
</html>