<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/index.css">
    <title>Job Hunter</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Lobster&display=swap" rel="stylesheet">
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</head>
<body>
    <div class="header">
        <h2>Job Hunter</h2>
        <div class="header-links">
            <button class="userjobs"><a href="/user/<%=user.id%>">Your Jobs</a></button>
            <form action="/logout?_method=DELETE" method="POST">
                <button type="submit" id="logout">Logout</button>
            </form>
        </div>
    </div>
    
    <div class="feature">
        <img src="/images/undraw_interview_re_e5jn.svg" alt="" id="main-image">
        <div class="feature-text">
            <h1>The Job of Your Dreams</h2>
            <h1>Now in the palm of your hands</h2>
        </div>
    </div>
    <div class="container">
        <div class="card">
            <div class="card-header">
              Featured
            </div>
            <div class="card-body">
              <h5 class="card-title"><%=featuredJob.role%></h5>
              <p class="card-text">Salary: <%=featuredJob.salary%></p>
              <p class="card-text">Company: <%=featuredJob.company%></p>
              <div class="row">
                <form action="/user/<%=user.id%>/job/<%=featuredJob.id%>?_method=PUT" method="POST" class="col"><button id="apply" class="btn btn-primary">Apply</button></form>
                <a class="btn btn-primary col" href="user/<%=user.id%>/job/<%=featuredJob.id%>">Details</a>
            </div>
            </div>
          </div>
    </div>
    
    <div class="container">
        <div class="row">
            <% jobs.forEach(job => { %>
                <div class="col-4 mb-4">
                    <div class="card rounded">
                        <div class="card-body">
                            <div class="card-title"><strong><%=job.role%></strong></div>
                            <div class="card-subtitle"><%=job.company%></div>
                            <div class="card-salary"><b>Salary: <%=job.salary %></b></div>
                            <div>Description:</div>
                            <div class="footer">
                                <div class="row">
                                    <form action="/user/<%=user.id%>/<%=job.id%>?_method=PUT" method="POST" class="col"><button id="apply" class="btn btn-primary">Apply</button></form>
                                    <a class="btn btn-primary col" href="user/<%=user.id%>/job/<%=job.id%>">Details</a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    </div>
    
    
</body>
</html>